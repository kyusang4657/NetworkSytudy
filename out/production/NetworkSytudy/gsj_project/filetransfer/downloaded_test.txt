UDP file transfer test file.
This file is intentionally longer than 512 bytes so the server must split it into multiple DATA packets.

[BLOCK TEST SECTION 1]
Line 001: The client sends an RRQ packet to request this file from the server.
Line 002: The server reads the requested file and divides the content into 512-byte chunks.
Line 003: Each chunk is encoded with Base64 before being placed inside a JSON DATA packet.
Line 004: The DATA packet contains the block number, encoded data, and the original chunk size.
Line 005: The client receives the DATA packet, decodes the payload, and writes it to an output buffer.

[BLOCK TEST SECTION 2]
Line 006: After receiving a DATA packet, the client sends an ACK packet with the same block number.
Line 007: The server waits for the ACK before sending the next DATA block.
Line 008: If the ACK is not received before the timeout, the server retransmits the last DATA packet.
Line 009: This repeated text makes the file large enough to test multiple packet transfers.
Line 010: If this content appears correctly in downloaded_test.txt, reconstruction is working.

[BLOCK TEST SECTION 3]
Line 011: Block boundaries may occur in the middle of a sentence, so the client must join bytes correctly.
Line 012: The final DATA packet should have a dataSize value smaller than 512 bytes.
Line 013: When the final block is received, the client exits the receive loop and saves the file.
Line 014: This confirms RRQ, DATA, ACK, Base64 decoding, and file writing are connected.
Line 015: End of multi-block UDP transfer test file.
