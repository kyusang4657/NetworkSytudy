# UDP 기반 파일 전송 클라이언트/서버 프로젝트 계획서

## 1. 문서 개요

| 항목 | 내용 |
|---|---|
| 문서명 | UDP 기반 파일 전송 클라이언트/서버 프로젝트 계획서 |
| 작성일 | 2026-06-10 |
| 개발 도구 | IntelliJ IDEA 2025.3.5 |
| 사용 기술 | Java, UDP DatagramSocket, JSON, Gson, Swing |
| 보조 도구 | ChatGPT, Codex |

본 문서는 UDP 기반 파일 전송 클라이언트/서버 시스템의 요구사항, 설계 방향, 구현 범위, 주요 동작 흐름, 테스트 계획 및 향후 개선 사항을 정리하기 위한 문서이다.

## 2. 프로젝트 개요

본 프로젝트는 UDP 프로토콜을 기반으로 클라이언트와 서버 간 파일 다운로드 및 업로드 기능을 제공하는 파일 전송 시스템이다.

클라이언트는 `RRQ(Read Request)` 패킷을 사용하여 서버에 저장된 파일을 요청하고 다운로드할 수 있다. 또한 `WRQ(Write Request)` 패킷을 사용하여 클라이언트의 로컬 파일을 서버에 업로드할 수 있다.

파일 데이터는 한 번에 전송하지 않고 512바이트 단위로 분할하여 전송한다. 각 데이터 블록은 `DATA` 패킷으로 전송되며, 수신 측은 각 블록에 대해 `ACK` 패킷을 응답한다. UDP는 전송 신뢰성을 보장하지 않기 때문에, 일부 전송 과정에서는 ACK 대기 및 timeout 재전송 방식을 사용하여 신뢰성을 보완한다.

## 3. 프로젝트 목표

본 프로젝트의 주요 목표는 다음과 같다.

| 구분 | 목표 |
|---|---|
| 통신 방식 | UDP 기반 클라이언트/서버 통신 구현 |
| 패킷 형식 | JSON 기반 패킷 구조 설계 및 구현 |
| 다운로드 | RRQ를 통한 서버 파일 다운로드 구현 |
| 업로드 | WRQ를 통한 클라이언트 파일 업로드 구현 |
| 파일 분할 | 파일을 512바이트 단위로 분할하여 전송 |
| 신뢰성 | DATA 패킷에 대한 ACK 응답 처리 |
| 오류 처리 | 잘못된 요청 및 파일 없음 상황에 대한 ERROR 패킷 처리 |
| GUI | Swing 기반 클라이언트 GUI 구현 |

## 4. 요구사항 분석

### 4.1 기능 요구사항

| ID | 요구사항 | 설명 | 현재 상태 |
|---|---|---|---|
| FR-001 | UDP 기반 통신 | DatagramSocket과 DatagramPacket을 사용하여 UDP 통신 수행 | 구현 |
| FR-002 | JSON 패킷 | 모든 요청과 응답을 JSON 형식으로 표현 | 구현 |
| FR-003 | RRQ 다운로드 | 클라이언트가 서버 파일을 요청하여 다운로드 | 구현 |
| FR-004 | WRQ 업로드 | 클라이언트 파일을 서버에 업로드 | 구현 |
| FR-005 | DATA 패킷 | 파일 데이터를 DATA 패킷으로 전송 | 구현 |
| FR-006 | 512바이트 블록 | 파일을 512바이트 단위로 분할하여 전송 | 구현 |
| FR-007 | ACK 처리 | DATA 수신 후 해당 블록 번호의 ACK 전송 | 구현 |
| FR-008 | Timeout 재전송 | ACK 미수신 시 마지막 DATA 패킷 재전송 | 부분 구현 |
| FR-009 | ERROR 처리 | 파일 없음, 잘못된 요청 등에 대해 ERROR 응답 | 구현 |
| FR-010 | Swing GUI | 파일 다운로드/업로드를 위한 클라이언트 GUI 제공 | 구현 |
| FR-011 | 인증 기능 | 허가된 사용자만 접근하도록 인증 처리 | 범위 제외 |

### 4.2 비기능 요구사항

| ID | 요구사항 | 설명 |
|---|---|---|
| NFR-001 | 안정성 | UDP의 비신뢰성을 ACK 기반 흐름으로 보완 |
| NFR-002 | 확장성 | RRQ, WRQ 외 추가 패킷을 확장할 수 있는 Packet 구조 사용 |
| NFR-003 | 사용성 | Swing GUI를 통해 파일명 입력, 저장 위치 선택, 업로드 파일 선택 지원 |
| NFR-004 | 유지보수성 | 서버와 클라이언트 로직을 메서드 단위로 분리 |

## 5. 시스템 설계

### 5.1 전체 구조

```text
[FileClientGUI]
      |
      v
[FileClient] <---- UDP / JSON Packet ----> [FileServer]
      |                                         |
 local file                              server file directory
```

### 5.2 주요 클래스

| 클래스 | 역할 |
|---|---|
| `FileServer` | UDP 서버 실행, RRQ/WRQ 요청 처리, 파일 송수신, ACK/ERROR 처리 |
| `FileClient` | RRQ/WRQ 요청 생성, 파일 다운로드/업로드, ACK 처리 |
| `FileClientGUI` | Swing 기반 클라이언트 GUI 제공 |
| `Packet` | JSON 패킷 데이터를 표현하는 모델 클래스 |
| `PacketUtil` | Gson을 사용하여 Packet 객체와 JSON 문자열을 변환 |
| `PacketType` | RRQ, WRQ, DATA, ACK, NACK, ERROR 패킷 종류 정의 |

## 6. 패킷 구조 설계

본 프로젝트에서는 모든 요청과 응답을 JSON 형식의 패킷으로 표현한다. 패킷은 공통적으로 `type` 필드를 가지며, 패킷 종류에 따라 `filename`, `block`, `data`, `message`, `dataSize` 등의 필드를 사용한다.

### 6.1 Packet 필드

| 필드 | 설명 |
|---|---|
| `type` | 패킷 종류 |
| `filename` | 요청하거나 저장할 파일 이름 |
| `block` | DATA 또는 ACK 패킷의 블록 번호 |
| `data` | Base64로 인코딩된 파일 데이터 |
| `message` | ERROR 패킷의 오류 메시지 |
| `dataSize` | DATA 패킷에 포함된 실제 파일 조각 크기 |

### 6.2 패킷 예시

RRQ 패킷:

```json
{
  "type": "RRQ",
  "filename": "test.txt",
  "block": 0,
  "data": null,
  "message": null,
  "dataSize": 0
}
```

WRQ 패킷:

```json
{
  "type": "WRQ",
  "filename": "upload.txt",
  "block": 0,
  "data": null,
  "message": null,
  "dataSize": 0
}
```

DATA 패킷:

```json
{
  "type": "DATA",
  "filename": null,
  "block": 1,
  "data": "Base64 encoded data",
  "message": null,
  "dataSize": 512
}
```

ACK 패킷:

```json
{
  "type": "ACK",
  "filename": null,
  "block": 1,
  "data": null,
  "message": null,
  "dataSize": 0
}
```

ERROR 패킷:

```json
{
  "type": "ERROR",
  "filename": null,
  "block": 0,
  "data": null,
  "message": "File not found",
  "dataSize": 0
}
```

## 7. 주요 동작 흐름

### 7.1 RRQ 다운로드 흐름

RRQ는 클라이언트가 서버에 저장된 파일을 읽기 위해 보내는 요청이다.

```text
1. 클라이언트가 서버에 RRQ(filename) 패킷을 전송한다.
2. 서버는 요청 파일명을 확인한다.
3. 서버는 BASE_DIR 기준으로 실제 파일 경로를 생성한다.
4. 파일이 없으면 ERROR 패킷을 전송한다.
5. 파일이 존재하면 서버는 파일을 512바이트 단위로 분할한다.
6. 서버는 각 블록을 Base64로 인코딩하여 DATA 패킷으로 전송한다.
7. 클라이언트는 DATA 패킷을 수신하고 Base64 데이터를 디코딩한다.
8. 클라이언트는 수신한 블록 번호에 대해 ACK 패킷을 전송한다.
9. 서버는 ACK를 확인한 뒤 다음 DATA 패킷을 전송한다.
10. 마지막 DATA 패킷의 dataSize가 512보다 작으면 전송을 종료한다.
11. 클라이언트는 누적된 데이터를 사용자가 선택한 경로에 파일로 저장한다.
```

### 7.2 WRQ 업로드 흐름

WRQ는 클라이언트가 로컬 파일을 서버에 저장하기 위해 보내는 요청이다.

```text
1. 클라이언트가 서버에 WRQ(filename) 패킷을 전송한다.
2. 서버는 파일명이 유효한지 확인한다.
3. 서버는 업로드 요청을 수락하면 ACK(0)을 전송한다.
4. 클라이언트는 로컬 파일을 512바이트 단위로 분할한다.
5. 클라이언트는 각 블록을 Base64로 인코딩하여 DATA 패킷으로 전송한다.
6. 서버는 DATA 패킷을 수신하고 Base64 데이터를 디코딩한다.
7. 서버는 수신한 DATA 블록 번호에 대해 ACK 패킷을 전송한다.
8. 마지막 DATA 패킷의 dataSize가 512보다 작으면 업로드를 종료한다.
9. 서버는 누적된 파일 데이터를 uploaded_파일명 형태로 저장한다.
```

## 8. 신뢰성 처리

UDP는 TCP와 달리 연결 설정, 패킷 순서 보장, 전달 보장, 재전송 기능을 제공하지 않는다. 따라서 본 프로젝트에서는 DATA 패킷에 대해 ACK를 사용하여 수신 여부를 확인한다.

RRQ 다운로드 과정에서는 서버가 DATA 패킷을 보낸 뒤 클라이언트의 ACK를 기다린다. 일정 시간 내 ACK를 수신하지 못하면 `SocketTimeoutException`이 발생하고, 서버는 마지막으로 전송했던 DATA 패킷을 재전송한다.

WRQ 업로드 과정에서는 클라이언트가 DATA 패킷을 보낸 뒤 서버의 ACK를 확인하고 다음 블록을 전송한다. 현재 WRQ 방향의 ACK timeout 재전송은 향후 개선 사항으로 남겨두었다.

## 9. GUI 설계

클라이언트는 Swing 기반 GUI를 제공한다.

### 9.1 다운로드 기능

사용자는 서버에 존재하는 파일명을 입력하고 `Download` 버튼을 클릭한다. 이후 `JFileChooser` 저장 대화상자를 통해 다운로드 파일을 저장할 경로를 선택한다.

```text
서버 파일명 입력
-> Download 클릭
-> 저장 위치 선택
-> RRQ 요청
-> 파일 다운로드
```

### 9.2 업로드 기능

사용자는 `Upload` 버튼을 클릭한 뒤 `JFileChooser`를 통해 로컬 파일을 선택한다. 선택된 파일은 WRQ 요청 이후 서버로 업로드된다.

```text
Upload 클릭
-> 로컬 파일 선택
-> WRQ 요청
-> 파일 업로드
```

### 9.3 GUI Thread 처리

Swing은 Event Dispatch Thread에서 화면을 처리한다. 네트워크 작업을 이벤트 스레드에서 직접 실행하면 GUI가 멈출 수 있으므로, 다운로드와 업로드 작업은 별도 Thread에서 수행하도록 구성하였다.

로그 출력은 `SwingUtilities.invokeLater()`를 사용하여 GUI 스레드에서 안전하게 갱신한다.

## 10. 멀티 클라이언트 처리

서버는 메인 UDP 소켓을 사용하여 클라이언트 요청을 수신한다. 요청이 들어오면 해당 요청 패킷을 복사한 뒤, 별도 Thread에서 처리한다.

각 요청 처리 스레드는 별도의 `DatagramSocket`을 생성하여 해당 클라이언트와 DATA/ACK 송수신을 수행한다. 이를 통해 메인 서버 소켓은 계속해서 새로운 클라이언트 요청을 받을 수 있다.

```text
main server socket
  - 9000번 포트에서 요청 수신

transfer socket
  - 요청별 Thread에서 생성
  - 특정 클라이언트와 파일 송수신 처리
```

요청 패킷을 Thread에 넘기기 전 `copyPacket()`으로 복사하는 이유는, 메인 서버 루프에서 사용하는 버퍼와 패킷 데이터가 다음 요청으로 인해 변경되는 상황을 방지하기 위해서이다.

## 11. 오류 처리

현재 구현된 오류 처리 방식은 다음과 같다.

| 상황 | 처리 방식 |
|---|---|
| filename이 없는 경우 | ERROR 패킷 전송 |
| 지원하지 않는 요청 type | ERROR 패킷 전송 |
| RRQ 요청 파일이 없는 경우 | ERROR 패킷 전송 |
| WRQ 중 DATA가 아닌 패킷 수신 | ERROR 패킷 전송 |

## 12. 테스트 계획

| 테스트 ID | 항목 | 방법 | 기대 결과 |
|---|---|---|---|
| TC-001 | RRQ 다운로드 | GUI에서 서버 파일명 입력 후 다운로드 | 선택한 경로에 파일 저장 |
| TC-002 | WRQ 업로드 | GUI에서 로컬 파일 선택 후 업로드 | 서버에 uploaded_파일명 생성 |
| TC-003 | 512바이트 초과 파일 다운로드 | 512바이트보다 큰 파일 다운로드 | 여러 DATA/ACK 로그 출력 |
| TC-004 | 없는 파일 다운로드 | 존재하지 않는 파일명 요청 | ERROR 메시지 출력 |
| TC-005 | 업로드 파일 비교 | 원본 파일과 uploaded_파일 비교 | 내용 일치 |
| TC-006 | GUI 동작 | 다운로드/업로드 버튼 클릭 | GUI가 멈추지 않고 작업 수행 |

## 13. 구현 상태

| 항목 | 상태 |
|---|---|
| UDP 통신 | 구현 |
| JSON 패킷 | 구현 |
| Gson 직렬화/역직렬화 | 구현 |
| RRQ 다운로드 | 구현 |
| WRQ 업로드 | 구현 |
| DATA 블록 전송 | 구현 |
| ACK 처리 | 구현 |
| RRQ timeout 재전송 | 구현 |
| WRQ timeout 재전송 | 향후 개선 |
| ERROR 처리 | 구현 |
| NACK 처리 | 향후 개선 |
| 인증 기능 | 범위 제외 |
| Swing GUI | 구현 |
| 서버 파일 목록 조회 | 향후 개선 |

## 14. 한계 및 향후 개선 사항

현재 구현은 과제 요구사항의 핵심인 UDP 기반 파일 다운로드/업로드, JSON 패킷, 512바이트 블록 전송, ACK 처리, ERROR 처리를 포함한다. 다만 다음 항목은 향후 개선 대상으로 남겨두었다.

```text
1. WRQ 업로드 방향의 ACK timeout 재전송 보강
2. NACK 패킷 기반 재전송 요청 구현
3. 서버 파일 목록 조회 기능 추가
4. 사용자 인증 기능 추가
5. 서버 저장 경로를 설정 파일 또는 실행 인자로 분리
6. 파일 경로 보안 검증 강화
7. 서버 GUI 또는 로그 관리 기능 추가
```

## 15. 결론

본 프로젝트는 UDP 기반 파일 전송 프로토콜의 핵심 동작을 직접 구현하는 것을 목표로 하였다. 클라이언트는 RRQ를 통해 서버 파일을 다운로드하고, WRQ를 통해 로컬 파일을 서버에 업로드할 수 있다. 파일은 512바이트 단위의 DATA 패킷으로 분할되며, 각 블록은 ACK를 통해 수신 여부를 확인한다.

또한 JSON 기반 패킷 구조와 Gson을 활용하여 패킷 생성 및 파싱을 단순화하였고, Base64 인코딩을 통해 바이너리 데이터를 JSON 문자열 안에 안전하게 포함할 수 있도록 하였다. Swing GUI를 통해 사용자가 파일 다운로드 및 업로드를 보다 쉽게 수행할 수 있도록 구성하였다.

이를 통해 UDP의 비신뢰성을 보완하기 위한 ACK, timeout, 재전송 개념과 파일 전송 프로토콜의 기본 구조를 이해하고 구현할 수 있었다.
